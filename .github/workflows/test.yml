name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
    - name: checksum
      run: |
        wget -q https://github.com/coelckers/ZMusic/archive/refs/tags/1.1.8.tar.gz
        ls -l
        shasum -a 256 *.tar.*

    # - name: test
    #   run: |
    #     sudo apt update
    #     sudo apt list --upgradable
