name: Test

on: [push]

jobs:
  build:
    runs-on: macos-12
    steps:
    - name: graphviz 
      run: |
        curl -JOL https://gitlab.com/api/v4/projects/4207231/packages/generic/graphviz-releases/8.1.0/graphviz-8.1.0.tar.gz
        tar -xf graphviz-8.1.0.tar.gz
        cd graphviz-8.1.0
        ./configure --help
  
   # runs-on: ubuntu-22.04
   # steps:
   # - name: checksum
   #   run: |
   #     links=( \
   #         https://gitlab.com/graphviz/graphviz/-/archive/8.1.0/graphviz-8.1.0.tar.bz2 \
   #     )
   #     for link in ${links[@]}; do
   #         wget -q $link
   #     done
   #     ls -l
   #     shasum -a 256 *
